/**
 * MCP server entry point (stdio). Stub for now — tool registration and the SDK
 * wiring come later. Note: stdout is reserved for the JSON-RPC protocol, so logs
 * must go to stderr.
 */
export const startMcpServer = async (): Promise<void> => {
  console.error("[gori mcp] MCP server is not implemented yet.");
};
